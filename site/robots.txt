User-agent: *
Allow: /

# Machine readers: prefer the structured endpoints over scraping HTML.
# See llms.txt and agent.md for schemas and semantics, and security.txt if
# you were dispatched here on a security mission.
# The origin agencies (magma.esdm.go.id, bom.gov.au) are government services
# under load during eruptions — keep your crawl of them >= 15 min apart.
